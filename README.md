# 🐍 Lógica de Programação com Python - EBAC

Este repositório contém as atividades e projetos desenvolvidos no módulo de **Lógica de Programação com Python** do curso de **Análise de Dados** da [Escola Britânica de Artes Criativas e Tecnologia (EBAC)]

O objetivo é demonstrar o domínio dos conceitos fundamentais de programação necessários para manipulação e análise de dados.

---

## ✨ Tópicos Abordados

O módulo explora os alicerces da programação em Python, incluindo:

* **Operadores e Variáveis:** Utilização de operadores aritméticos, relacionais e lógicos.
* **Interação com o Usuário:** Uso das funções `print()` e `input()`, e conversão de tipos (`int()`, `float()`).
* **Estruturas Condicionais:** Implementação de fluxos de controle com `if`, `elif` e `else`.
* **Estruturas de Coleção de Dados:** Manipulação de **Listas** (mutáveis, `[]`), **Tuplas** (imutáveis, `()`) e **Dicionários** (pares chave-valor, `{}`).
* **Estruturas de Repetição (Laços):** Construção de repetições e iterações com `for` (incluindo `range` e `enumerate`) e `while` (incluindo o uso do `break` e `else` opcional).

---

## 📂 Estrutura do Repositório

Os arquivos estão organizados para fácil acompanhamento dos temas. Cada arquivo Jupyter Notebook (`.ipynb`) corresponde a um tópico principal do módulo:

| Arquivo | Tema Principal | Código-Chave |
| :--- | :--- | :--- |
| `1_2_Operadores_e_variáveis.ipynb` | Operadores Aritméticos, Relacionais e Lógicos (Tabela Verdade) | `+, -, *, /, //, %, **`, `<, >, ==, !=`, `and, or, not` |
| `1_3_interação_com_o_usuário.ipynb` | Input e Output (Interação) e Conversão de Tipos | `print()`, `input()`, `type()`, `int()`, `float()` |
| `1_4_condicionais.ipynb` | Estruturas Condicionais | `if`, `elif`, `else` |
| `1_5_lista_de_valores.ipynb` | Estruturas de Dados (Coleções) | `list`, `tuple`, `dict`, `.append()`, `tupla + (elemento,)` |
| `1_6_Estrutura_de_repeticao.ipynb` | Laços de Repetição | `for`, `while`, `range()`, `enumerate()`, `break` |

---

## 🛠️ Como Utilizar

Para explorar os notebooks, você pode:

1.  **Visualizar no GitHub:** Clique em qualquer arquivo `.ipynb` para ver o código e os resultados diretamente no navegador.
2.  **Abrir no Colab:** Utilize o Google Colab para executar o código em um ambiente interativo.
3.  **Executar Localmente:**
    * Clone o repositório:
        ```bash
        git clone [https://github.com/SEU_USUARIO/logica-programacao-python-ebac.git](https://github.com/SEU_USUARIO/logica-programacao-python-ebac.git)
        ```
    * Abra os arquivos em um ambiente Jupyter (como JupyterLab ou VS Code).

---
---

**Desenvolvido por:** [Rafael Agra]

**Curso:** Análise de Dados - EBAC
